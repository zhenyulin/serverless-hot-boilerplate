---
language: node_js
node_js:
  - 8.10
cache:
  yarn: true
  directories:
    - ~/.npm
    - ~/.nvm
    - node_modules
script:
  - npm install -g serverless npx
  - make install
  - make test-coverage
  - cat coverage/lcov.info | npx coveralls
  - make deploy
branches:
  only:
    - master

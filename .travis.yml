---
language: node_js
node_js:
  - 8.10
cache:
  yarn: true
  directories:
    - ~/.npm
    - node_modules
script:
  - npm install -g serverless npx
  - make install
  - make test-coverage | npx coveralls
  - make deploy
branches:
  only:
    - master

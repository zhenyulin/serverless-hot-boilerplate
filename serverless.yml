---
service: todo-example

frameworkVersion: ">=1.1.0 <2.0.0"

resources:
  Resources:
    TodosDynamoDbTable:
      Type: 'AWS::DynamoDB::Table'
      Properties:
        AttributeDefinitions:
          -
            AttributeName: id
            AttributeType: S
        KeySchema:
          -
            AttributeName: id
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
        TableName: serverless-example-todos

provider:
  name: aws
  runtime: nodejs8.10
  stage: dev
  region: eu-west-1
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource:
        Fn::GetAtt:
          - TodosDynamoDbTable
          - Arn
  environment:
    TODOS_TABLE_NAME: serverless-example-todos

functions:
  create:
    handler: todos/create.handler
    events:
      - http:
          path: todos
          method: post
          cors: true

  list:
    handler: todos/list.handler
    events:
      - http:
          path: todos
          method: get
          cors: true

  get:
    handler: todos/get.handler
    events:
      - http:
          path: todos/{id}
          method: get
          cors: true

  update:
    handler: todos/update.handler
    events:
      - http:
          path: todos/{id}
          method: put
          cors: true

  delete:
    handler: todos/delete.handler
    events:
      - http:
          path: todos/{id}
          method: delete
          cors: true
